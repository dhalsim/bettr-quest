import CoachDirectory from '@/components/coach/CoachDirectory';

const CoachDirectoryPage = () => {
  return <CoachDirectory showRegisterButton={true} mode="link" />;
};

export default CoachDirectoryPage;
